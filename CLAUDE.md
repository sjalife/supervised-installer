# CLAUDE.md — supervised-installer

> **Global conventions live in the personal workspace master at `../Claude/CLAUDE.md`.**
> Read it for working language, the GitHub sync workflow, the secrets discipline
> (required `.gitignore` block + pre-push verification), and the handover-note pattern.
> This file adds project-specific context and overrides where they conflict.

## What this repo is

Installer for a generic Linux system (shell). Fork of `home-assistant/supervised-installer`.

## Project-specific notes

_Public fork — keep upstream-mergeable changes separate from personal experiments._
